import { css } from '@emotion/css';

const CommonDemo = () => {
  return (
    <div
      className={css({
        color: 'green',
      })}
    >
      CommonDemo
    </div>
  );
};

export default CommonDemo;
